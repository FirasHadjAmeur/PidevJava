/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.esprit.test;

import com.esprit.modeles.Cart;
import com.esprit.modeles.Livreur;
import com.esprit.modeles.Products;
import com.esprit.services.ServiceCartt;
import com.esprit.services.ServiceLivreur;
import com.esprit.utils.DataSource;
import static java.time.Clock.system;
import java.util.Scanner;

/**
 *
 * @author azizm
 */
public class main {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        
     DataSource ds1 = DataSource.getInstance();
     DataSource ds2 = DataSource.getInstance();
  // Scanner  rs = new Scanner(System.in);
   //   ServiceCartt sc=new ServiceCartt();
     // Produit p1=new Produit(1,"tshirt",100);
     //Cart t=new Cart(1,p1.getId());
     // sc.ajouter(t);
      
      //sc.afficher().forEach(System.out::println);
      
    // ServiceLivreur sp=new ServiceLivreur();
     // ServiceLivreur st=new ServiceLivreur ();
// Livreur livreur =new Livreur("mohamed","aziz","aaaa","6") ;
 //String g = rs.nextLine();
  //String gc = rs.nextLine();
  // String gcc = rs.nextLine();
  //String gccc = rs.nextLine();
 //livreur.setNomLivreur(g);
 //livreur.setPrenomLivreur(gc);
 //livreur.setEmail(gcc);
 //livreur.setNumTelephone(gccc);
 //livreur.setIdLivreur(2);
 //st.modifier(livreur);
   //sp.ajouter(new Livreur("aziz","maamer","aziz.maamer@gmail.com","2"));
     
    //  sp.afficher().forEach(System.out::println);

        
      
    }
    
}
